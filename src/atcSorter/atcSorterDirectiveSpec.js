describe('atcSorterDirective', function () {

    var instance,
        $scope;

    beforeEach(function () {
        module("ui-template");

        inject(function($rootScope) {
            $scope = $rootScope.$new();
        });
    });

    describe('initialization', function () {
    });
});
