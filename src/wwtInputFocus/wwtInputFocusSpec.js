describe('wwtInputFocus', function () {

    var instance;

    beforeEach(function () {
        module("ui-template");

        inject(function(wwtInputFocus) {
            instance = wwtInputFocus;
        });

    });
    describe('on load,', function () {

    });
});
